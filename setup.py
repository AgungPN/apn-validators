from setuptools import setup, find_packages

setup(
    name="apn-validators",
    version="0.2",
    packages=find_packages(),
    install_requires=[
        # dependencies we need to install
    ],
)
